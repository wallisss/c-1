#include <stdio.h>
/*  operator (unary) + - >>> 1 operand -1 +1
    Arithmetic(binary) + - / * % >>> 
    Increment (unary) ++ --
    Assigment(binary) = += -= /= *= %= >>>
    Comparision(binary) == != < > <= >=
    Logical(binary) && || !
    Conditional(ternary) ?: 

    Reference - https://en.cppreference.com/w/c/language/operator_precedence 
    */

int main()
{

    return 0;
}